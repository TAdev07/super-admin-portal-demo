{
  "name": "mini-portal-mf",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "rm -rf dist bundle.zip",
    "build": "tsc -b && vite build",
    "bundle": "npm run clean && npm run build && cd dist && zip -r ../bundle.zip . && cd ..",
    "preview": "vite preview",
    "type-check": "tsc -b --pretty"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.3.6",
    "@types/node": "^24.3.1",
  "@types/react": "^19.1.10",
  "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
